"""Time series forecasting with uncertainty.

Core library: pure, deterministic, testable functions. No plotting and no
file writes inside the library modules — plotting lives in ``dashboard/`` and
artifact writing lives in ``scripts/``.
"""

__version__ = "0.1.0"
